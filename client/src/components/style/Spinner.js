import spin from './spinner.gif'

function Spinner() {
    return (
        <div>
              <img src={spin} style={{margin:'15% 38%'}} alt='spinner' />
        </div>
    )
}

export default Spinner
